# Personal applications and utilities.  A failure here never prevents the
# desktop, shell, dotfiles, or session from being configured.
anytype-bin
downgrade
gtk-engine-murrine
openai-chatgpt
qt5-webengine
slack-desktop-wayland
spotify
stremio
ttf-victor-mono
visual-studio-code-bin
# The repository package `waybar` is the supported desktop dependency.  The
# -git variant is deliberately optional to avoid replacing it during bootstrap.
waybar-git
