# Theme generation is a first-class part of this profile.  The installer keeps
# going if it is temporarily unavailable because committed generated colors are
# also installed, then reports the warning clearly.
wallust
